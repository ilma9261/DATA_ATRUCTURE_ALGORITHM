// gfg    &&     // leetcode 209


class Solution {
  public:
    int smallestSubWithSum(int x, vector<int>& arr) {
        // code here
        int mini=INT_MAX;
        int i=0;
        int j=0;
        int sum=0;
        int n=arr.size();
        while(j<n){
            sum+=arr[j];
            while(sum>x){
                mini=min(mini,j-i+1);         // subarray length= j-i+1
                sum=sum-arr[i];
                i++;
            }
                j++;
        }
        if(mini==INT_MAX){
            return 0;
        }
        else{
            return mini;
        }https://www.geeksforgeeks.org/community/profile/hire1/
    }
};


class Solution {
public:
    int minSubArrayLen(int target, vector<int>& a) {
        int sum=0;
        int n=a.size();
        int low=0;
        int high=0;
        int res=INT_MAX;
        while(high<n){
            sum=sum+a[high];
            while(sum>=target){
               int len=high-low+1;
                res=min(res,len);
                sum=sum-a[low];
                low++;
            }
            high++;
        }
        if(res==INT_MAX){
            return 0;
        }
        return res;
    }
};
