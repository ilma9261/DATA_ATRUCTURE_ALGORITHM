//gfg
// two methods

//without map

class Solution {
public:
    int getCount(string s, int k) {
        int count = 1;
        int res = 0;
        for (int i = 1; i < s.size(); i++) {
            if (s[i] == s[i - 1]) {
                count++;
            } else {
                if (count == k)
                    res++;
                count = 1;
            }
        }
        // Check the last group
// Whenever you process something when it changes, the last group is left unprocessed because it never changes again.
// So after the loop, always ask: "Is there one last group still waiting to be processed?"
        if (count == k)
            res++;
        return res;
    }
};


// with map

class Solution {
  public:
    int getCount(string s, int k) {
        // code here
        int n=s.size();
        int res=0;
        unordered_map<char,int> mp;
            mp[s[0]]++;
        for(int i=1;i<n;i++){
            if(s[i]!=s[i-1]){
            mp[s[i]]++;
            }
        }
        for (int i=0;i<n;i++){
            if(mp[s[i]]==k){
                res++;
                mp.erase(s[i]);          // keep this in mind
            }
        }
        return res;
    }
};
