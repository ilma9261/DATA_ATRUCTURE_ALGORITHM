// leetcode 1614

class Solution {
public:
    int maxDepth(string s) {
        int n=s.size();
        int depth=0;
        int bracket=0;
        for(int i=0;i<n;i++){
            if(s[i]=='('){
                depth++;
                bracket=max(bracket,depth);
            }
            if(s[i]==')'){
                depth--;
            }
        }
        return bracket;
    }
};
