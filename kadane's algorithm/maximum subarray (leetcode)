//leetcode 53
//sliding window does same work but fails for negative elements.

class Solution {
public:
    int maxSubArray(vector<int>& a) {
        int i=1;
        int n=a.size();
       int bestending=a[0];
       int ans=a[0];
        for(i=1;i<n;i++){
            int v1= bestending + a[i];
            int v2=a[i];
            bestending =max(v1,v2);
            ans=max(ans,bestending);
        }
        return ans;
    }
};
