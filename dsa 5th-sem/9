#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main() {
int n,x;
cin>>n>>x;
string totalBill=to_string(n*x);
if(totalBill.size()==5 && totalBill[0]!=0){
    cout<<"YES";
}
else{
    cout<<"NO";
}
return 0;
}
