// gfg


class Solution {
  public:
    vector<vector<int>> countFreq(vector<int>& arr) {
        // code here
        int n=arr.size();
        vector<vector<int>> v;
        unordered_map<int,int> mp;
        for (int i=0;i<n;i++){
            mp[arr[i]]++;
        }
       
        for(int i=0;i<n;i++){
           if (mp.find(arr[i]) != mp.end()) {             // element present
                v.push_back({arr[i], mp[arr[i]]});
                mp.erase(arr[i]);                         // remove it after adding
            }
        }
        return v;
    }
};
