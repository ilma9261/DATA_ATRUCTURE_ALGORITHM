//gfg
//two pointer

// LEFT 

class Solution {
  public:
    void rotateArr(vector<int>& arr, int d) {
        int n=arr.size();
        d=d%n;                    // If d is bigger than the array size, make it smaller because rotating more than n times repeats the same pattern.   d = 2 % 5 = 2   ,    d = 7 % 5 = 2
        int i=0;
        int j=d-1;
        while(i<j){
            swap(arr[i],arr[j]);
            i++;
            j--;
        }
        i=d;
        j=n-1;
        while(i<j){
            swap(arr[i],arr[j]);
            i++;
            j--;
        }
        i=0;
        j=n-1;
        while(i<j){
            swap(arr[i],arr[j]);
            i++;
            j--;
        }
    }
};



// LEETCODE 189

// RIGHT 

class Solution {
public:
    void rotate(vector<int>& nums, int k) {
        int n=nums.size();
        k=k%n;
        int i=0;
        int j=n-1;
        while(i<j){
            swap(nums[i],nums[j]);
            i++;
            j--;
        }
           i=0;
           j=k-1;
        while(i<j){
            swap(nums[i],nums[j]);
            i++;
            j--;
        }
         i=k;
         j=n-1;
        while(i<j){
            swap(nums[i],nums[j]);
            i++;
            j--;
        }
    }
};
